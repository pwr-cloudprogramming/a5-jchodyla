version: '2.0'
services:
  frontend:
    image: front
    build:
      context: frontend
      dockerfile: Dockerfile
      args:
        - IP=${IP}
    ports:
      - 8081:3000
  backend:
    image: back
    build:
      context: backend
      dockerfile: Dockerfile
    args:
        - IP=${IP}
    ports:
      - 8080:8080
      